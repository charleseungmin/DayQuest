# W-16 Dev Progress (2026-02-17 16:35 KST)

## Selected P0 기능 태스크
1. **P0-HISTORY-LIVE-01**: History 화면을 실제 DB 요약 데이터와 연동
2. **P0-HISTORY-FLOW-02**: History 화면에 반응형(Flow) 집계 파이프라인 연결

## 실제 기능 구현
- `DailyItemDao`에 반응형 집계 쿼리 추가
  - `observeCountByDate(dateKey)`
  - `observeCountByDateAndStatus(dateKey, status)`
  - `observeCountByDateRangeAndStatus(startDateKey, endDateKey, status)`
- `ObserveHistorySummaryUseCase` 신규 추가
  - 오늘 완료/총 개수, 주간 누적 완료 개수를 Flow 결합으로 계산
- `HistoryViewModel` 신규 추가
  - Loading/Error/Ready 상태 관리
  - `refresh()` 재시도 플로우 제공
- `HistoryScreen`을 목업 고정값에서 ViewModel 상태 기반 UI로 교체
  - 로딩 카드 / 에러 카드(재시도) / 실제 집계 카드 렌더링
- `TaskModels`에 `HistoryUiState` 추가

## 검증
- 실행: `./gradlew.bat :app:compileDebugKotlin`
- 결과: **BUILD SUCCESSFUL**

## 비고
- 기존 워킹트리에 선행 변경사항이 많은 상태에서, 이번 실행 범위는 History 실데이터 연동 기능 파일 중심으로 반영함.
