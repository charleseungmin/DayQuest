# W-02 Wireframe Sync (2026-02-16 22:33 KST)

- 기준 프로젝트 경로: `C:\Users\MYCOM\StudioProjects\DayQuest`
- 작업 범위: 코드/문서 기반 와이어프레임 정리 (Figma 라이브 조작 제외)

## 이번 실행의 실제 변경

### 1) 화면 헤더 블록 공통 컴포넌트 도입
대상 파일: `app/src/main/java/com/dayquest/app/MainActivity.kt`

- 신규 컴포저블: `ScreenSectionHeader(title, subtitle)`
- 목적: 화면별 상단 라벨 영역을 Figma 프레임의 Header 블록처럼 동일한 카드 패턴으로 통일

### 2) 화면별 헤더를 공통 블록으로 치환
대상 파일: `app/src/main/java/com/dayquest/app/MainActivity.kt`

- `TaskManageScreen`: `Text("오늘의 Task")` → `ScreenSectionHeader("오늘의 Task", "등록 · 진행 · 완료 흐름")`
- `HistoryScreen`: `Text("기록 조회")` → `ScreenSectionHeader("기록 조회", "일간/주간 진행 요약")`
- `SettingsScreen`: `Text("설정")` → `ScreenSectionHeader("설정", "알림/초기화 옵션")`

## 와이어프레임 블록 매핑(업데이트)

- Global / Screen Header Block → `ScreenSectionHeader`
- Task Manage / State Switcher → `TaskStateDebugCard`
- Task Manage / Progress Summary → `QuestProgressCard`
- Task Manage / Input Form → `TaskFormCard`
- Task Manage / Task List → `TaskListCard`

## 검증

- 실행 명령: `./gradlew.bat :app:compileDebugKotlin`
- 결과: `BUILD SUCCESSFUL`
