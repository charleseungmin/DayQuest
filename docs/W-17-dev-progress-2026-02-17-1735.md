# W-17 Dev Progress (2026-02-17 17:35 KST)

## Selected P0 기능 태스크
1. **P0-SETTINGS-LIVE-01**: Settings 화면을 샘플 로컬 상태가 아닌 실제 앱 설정 상태와 연동
2. **P0-SETTINGS-FLOW-02**: 데이터 초기화 버튼을 실제 로컬 DB 초기화 플로우와 연결하고 결과/오류 UX 반영

## 실제 기능 구현
- `SettingsRepository`/`SettingsRepositoryImpl` 신규 추가
  - 알림 사용 여부를 SharedPreferences 기반으로 저장/조회
  - 상태를 Flow(`MutableStateFlow`)로 노출
  - `DayQuestDatabase.clearAllTables()`를 통한 실제 로컬 DB 초기화 구현
- Settings 도메인 UseCase 3종 추가
  - `ObserveNotificationEnabledUseCase`
  - `SetNotificationEnabledUseCase`
  - `ResetLocalDataUseCase`
- `SettingsViewModel` 신규 추가
  - 설정 상태 구독/로딩 처리
  - 알림 토글 저장 처리
  - 초기화 실행 상태(`isResetting`) + 완료/오류 메시지 처리
- `SettingsScreen`을 ViewModel 기반으로 교체
  - `hiltViewModel()` 연동
  - 샘플 문구 제거 및 실제 초기화 동작 안내 문구 반영
  - 초기화 중 버튼 비활성화, 완료/오류 텍스트 및 Snackbar 노출
- DI 바인딩 추가
  - `SettingsModule`에서 `SettingsRepositoryImpl -> SettingsRepository` 바인딩
- UI 모델 확장
  - `SettingsUiState`에 `isLoading`, `isResetting`, `errorMessage`, `noticeMessage` 필드 추가

## 검증
- 실행: `./gradlew.bat :app:compileDebugKotlin`
- 결과: **BUILD SUCCESSFUL**

## 비고
- 기존 워킹트리에 선행 변경사항이 많은 상태에서, 이번 실행은 Settings 실동작 연동(P0) 기능 파일 중심으로 반영함.
