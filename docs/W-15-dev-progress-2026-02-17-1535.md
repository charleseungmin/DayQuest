# W-15 Dev Progress (2026-02-17 15:35 KST)

## Selected P0 기능 태스크
1. **P0-TODAY-LIVE-01**: Today 화면을 실제 DB 데이터(daily_items + tasks)와 연동
2. **P0-TODAY-FLOW-02**: Today 화면에서 완료/되돌리기 상태를 실제 DailyItem 상태 변경으로 연결

## 실제 기능 구현
- `DailyItemDao`에 Today 조인 조회 `observeTodayTasks(dateKey)` 추가
- `TodayTaskRow` projection 추가
- `ObserveTodayTasksUseCase` 신규 추가
- `TodayViewModel` 신규 추가
  - 앱 실행 시 오늘 날짜 daily item 생성(idempotent)
  - Today 목록 실시간 구독
  - 완료 토글 시 `UpdateDailyItemStatusUseCase`로 DONE/TODO 반영
- `TodayScreen`을 목업 카드 기반에서 실제 상태 기반 UI로 교체
  - Loading / Error / Ready 상태 처리
  - 실시간 진행도 카드 + Today 할 일 목록 표시
- `TaskListCard`에 `showEditDeleteActions` 옵션 추가
  - Today 화면에서는 수정/삭제 액션 숨김(완료 토글 전용)

## 검증
- 실행: `./gradlew.bat :app:testDebugUnitTest --tests "*UpdateDailyItemStatusUseCaseTest" --tests "*GenerateTodayItemsUseCaseTest"`
- 결과: **BUILD SUCCESSFUL**

## 비고
- 기존 미정리 변경사항(다른 파일/문서)은 워킹트리에 유지된 상태로 이번 기능 추가를 수행함.
