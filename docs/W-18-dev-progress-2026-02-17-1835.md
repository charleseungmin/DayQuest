# W-18 Dev Progress (2026-02-17 18:35 KST)

## Selected P0 기능 태스크
1. **P0-TODAY-MANAGE-FLOW-01**: Today 화면에서 실제 Task 수정 흐름으로 진입 가능하도록 네비게이션 연결
2. **P0-TODAY-EMPTY-CTA-02**: Today 빈 상태를 실제 기능 흐름(Manage 이동)과 연결

## 실제 기능 구현
- Today → Manage 편집 진입 플로우 연결
  - `DayQuestNavHost`에 `manage?editTaskId={editTaskId}` 라우트 추가
  - Today 목록의 `수정` 액션 클릭 시 해당 taskId를 쿼리 파라미터로 전달하여 Manage로 이동
- Manage 화면 초기 편집 대상 주입
  - `TaskManageScreen(initialEditTaskId)` 파라미터 추가
  - `LaunchedEffect`에서 `TaskManageViewModel.requestEdit()` 호출
  - ViewModel에 `pendingEditTaskId` 처리 추가하여 데이터 로딩 타이밍과 무관하게 편집 폼 자동 채움
- Today 빈 상태의 실제 액션 연결
  - Today tasks 비어 있을 때 안내 카드 + `Manage로 이동` 버튼 노출
  - 버튼 클릭 시 Manage 화면으로 이동
- 컴포넌트 액션 제어 확장
  - `TaskListCard`에 `showEditAction`, `showDeleteAction` 분리 옵션 추가
  - Today에서는 수정만 노출(삭제 버튼 비노출), Manage는 기존 수정/삭제 모두 유지

## 검증
- 실행: `./gradlew.bat :app:compileDebugKotlin`
- 결과: **BUILD SUCCESSFUL**

## 변경 파일(핵심)
- `app/src/main/java/com/dayquest/app/ui/navigation/DayQuestNavHost.kt`
- `app/src/main/java/com/dayquest/app/ui/screen/TodayScreen.kt`
- `app/src/main/java/com/dayquest/app/ui/screen/TaskManageScreen.kt`
- `app/src/main/java/com/dayquest/app/ui/screen/TaskManageViewModel.kt`
- `app/src/main/java/com/dayquest/app/ui/component/CommonComponents.kt`
