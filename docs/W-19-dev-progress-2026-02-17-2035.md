# W-19 Dev Progress (2026-02-17 20:35 KST)

## Selected P0 기능 태스크
1. **P0-TODAY-DEFER-FLOW-03**: Today 화면에서 DailyItem 상태를 `미룸(DEFERRED)`으로 전환/복원하는 실제 액션 연결
2. **P0-TODAY-DEFER-UI-04**: Today 목록에 `미룸` 상태를 노출하고 진행중/완료/미룸 상태를 UI에서 구분 표시

## 실제 기능 구현
- `TodayViewModel`
  - `toggleDeferred(taskId, isDeferred)` 추가
  - `UpdateDailyItemStatusUseCase`를 통해 `TODO ↔ DEFERRED` 상태 전환 연결
  - `TodayTaskRow -> TaskItemUi` 매핑에 `isDeferred` 반영
- `TaskModels`
  - `TaskItemUi`에 `isDeferred` 필드 추가
- `CommonComponents.TaskListCard`
  - 상태 라벨 확장: `진행중 / 완료 / 미룸`
  - Defer 액션 확장: `onDefer`, `showDeferAction`
  - 미룸 상태에서는 버튼 텍스트 `복원`, 일반 상태에서는 `미루기`
- `TodayScreen`
  - Today 리스트에서 `onDefer`를 ViewModel `toggleDeferred`와 연결
  - Today에서 `showDeferAction = true`로 실제 미루기 플로우 활성화

## 최소 검증 테스트
- 실행: `./gradlew.bat :app:compileDebugKotlin`
- 결과: **BUILD SUCCESSFUL**

## 변경 파일(핵심)
- `app/src/main/java/com/dayquest/app/ui/screen/TodayViewModel.kt`
- `app/src/main/java/com/dayquest/app/ui/screen/TodayScreen.kt`
- `app/src/main/java/com/dayquest/app/ui/component/CommonComponents.kt`
- `app/src/main/java/com/dayquest/app/ui/model/TaskModels.kt`
