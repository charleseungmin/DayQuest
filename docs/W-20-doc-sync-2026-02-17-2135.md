# W-20 PR / Notion Sync (2026-02-17 21:35 KST)

## PR 반영 대상
- Today에서 항목 편집 시 올바른 원본 Task를 Manage 편집 폼으로 여는 라우팅 정합성 수정
- History에서 완료 수치만 보이던 요약에 `미룸` 집계를 추가해 상태 가시성 강화

## Notion 동기화 항목
- `P0-TODAY-EDIT-LINK-05`: In Progress → 구현 완료(코드 반영)
- `P0-HISTORY-DEFERRED-SUMMARY-06`: In Progress → 구현 완료(코드 반영)

## 테스트
- `./gradlew.bat :app:compileDebugKotlin`
- 결과: PASS (BUILD SUCCESSFUL)

## 전달 메모
- 현재 워킹트리에 선행 변경이 다수 존재하므로 PR 생성 시 이번 작업 파일만 범위 분리 필요.
- Notion API 직접 반영은 실행 컨텍스트 제약으로 미수행, 문서 로그 기준으로 동기화.
